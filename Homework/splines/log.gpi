set terminal svg background "white" ;set out "int.gnuplot.svg" ;set xlabel "x" ;set ylabel "Integral from x[0]" ;set tics out ;set grid ;set key top right;f(x)=sin(x);plot[0:9] 	"data.txt" index 1 using 1:3 with lines title "Linear interpolation" 	, "data.txt" index 2 using 1:3 with lines title "Quadratic splines", f(x) with lines title "Sin(x) (The definite integral from x[0] to x of cos(x))"  ;	 
