set terminal svg background "white" ;set out "times.gnuplot.svg" ;set xlabel "N" ;set ylabel "time [s]" ;set tics out ;set grid ;set key top right;f(x) = a*x*x*x ;fit f(x) "out.times.data" using 1:2 via a ;plot 	"out.times.data" index 0 with points title "QR-decomposition time as a function of N", f(x) with lines title "fitted a*N^3" ;	 
