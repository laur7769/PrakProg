set terminal svg background "white" ;set out "times.gnuplot.svg" ;set xlabel "N" ;set ylabel "time [s]" ;set tics out ;set grid ;set key top right;f(x) = a*x*x ;g(x) = b*x*x*x;fit f(x) "out.times-2.txt" using 1:2 via a ;fit g(x) "out.times-2.txt" using 1:2 via b ;plot 	"out.times-2.txt" index 0 with points title "Measured time of diagonalization as a function og matrix size" 	, f(x) with lines title "a*N^3 fitted to data", g(x) with lines title "b*N^2 fitted to data";	 
